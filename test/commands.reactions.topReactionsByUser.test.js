const discordBot = require("../src/configure/discordBot");
const topReactionsByUser = require("../src/commands/reactions/topReactionsByUser").callback;


describe('Command topReactionsByUser.js tests', () => {
  it('Command /topReactionsByUser ???', async () => {});
  
  
    /*
    it('Command /topReactionsByUser ???', async () => {
      
      // Arrange
      discordBot.initDiscordBotClient();
      const message = {
        reply: jest.fn(),
        content: 'topreactionsbyuser'
      };
      
      const expected = {
          content: 'pong'
      };
      
            
      // Act
      await topReactionsByUser(discordBot.client, message);
      
      // Assert
      expect(message.reply).toHaveBeenCalledWith(expected);
      
    });*/
    
});
